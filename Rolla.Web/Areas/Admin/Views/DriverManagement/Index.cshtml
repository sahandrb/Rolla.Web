@using Rolla.Domain.Entities
@using Rolla.Domain.Enums
@model List<ApplicationUser>

@{
    ViewData["Title"] = "مدیریت رانندگان";
}

<div class="container mt-4">
    <h2>لیست رانندگان</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ایمیل</th>
                <th>ماشین</th>
                <th>پلاک</th>
                <th>وضعیت فعلی</th>
                <th>عملیات</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var driver in Model)
            {
                <tr>
                    <td>@driver.Email</td>
                    <td>@driver.CarModel</td>
                    <td>@driver.PlateNumber</td>
                    <!-- ... بقیه کدها ... -->
                    <td>
                        @if (driver.DriverStatus == DriverStatus.Pending)
                        {
                            <!-- دکمه تایید -->
                            <form asp-area="Admin" asp-controller="DriverManagement" asp-action="Approve" method="post" style="display:inline;">
                                <input type="hidden" name="userId" value="@driver.Id" />
                                <button type="submit" class="btn btn-sm btn-success">✅ تایید</button>
                            </form>

                            <!-- دکمه رد -->
                            <form asp-area="Admin" asp-controller="DriverManagement" asp-action="Reject" method="post" style="display:inline;">
                                <input type="hidden" name="userId" value="@driver.Id" />
                                <button type="submit" class="btn btn-sm btn-danger">❌ رد</button>
                            </form>
                        }
                        else
                        {
                            <!-- اگر وضعیت چیزی غیر از Pending بود، دکمه‌ها را نشان نده یا غیرفعال کن -->
                            <span class="text-muted">عملیات تکمیل شده</span>
                        }
                    </td>
                    <!-- ... بقیه کدها ... -->
                    <td>
                        @if (driver.DriverStatus == DriverStatus.Pending)
                        {
                            <form asp-action="Approve" method="post" style="display:inline;">
                                <input type="hidden" name="userId" value="@driver.Id" />
                                <button class="btn btn-sm btn-success">✅ تایید</button>
                            </form>
                            <form asp-action="Reject" method="post" style="display:inline;">
                                <input type="hidden" name="userId" value="@driver.Id" />
                                <button class="btn btn-sm btn-danger">❌ رد</button>
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>